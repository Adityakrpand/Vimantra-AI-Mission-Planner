# API

This folder documents backend API contracts, endpoint behavior, request/response models, error semantics, and compatibility expectations.

During local development, interactive API documentation is available from the running backend at `http://127.0.0.1:8000/docs`.
