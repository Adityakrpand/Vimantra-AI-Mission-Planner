# Missions

This folder is reserved for local mission artifacts and exported mission files.

The application stores its default SQLite mission database under `database/`. Files placed here should be explicit examples, exports, or operator-provided local artifacts. Do not commit sensitive operational mission data.
